[package]
name = "lattice-safe"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["lattice-safe contributors"]
description = "Post-quantum cryptography suite — ML-DSA (FIPS 204) + FN-DSA (FIPS 206) digital signatures"
license = "MIT"
readme = "README.md"
repository = "https://github.com/lattice-safe/lattice-safe"
homepage = "https://github.com/lattice-safe"
documentation = "https://docs.rs/lattice-safe"
keywords = [
    "post-quantum",
    "cryptography",
    "digital-signature",
    "ml-dsa",
    "fn-dsa",
]
categories = ["cryptography", "no-std"]

[features]
default = ["dilithium", "falcon"]
dilithium = ["dep:dilithium-rs"]
falcon = ["dep:falcon-rs"]
serde = ["dilithium-rs?/serde", "falcon-rs?/serde"]
simd = ["dilithium-rs?/simd"]

[dependencies]
dilithium-rs = { version = "0.2", optional = true }
falcon-rs = { version = "0.2", optional = true }

[package.metadata.docs.rs]
all-features = true
